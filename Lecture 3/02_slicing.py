str = "python"
# Positive indexing it always run into n-1 :
# str = [startIdx:endIdx] endInx not include. 

print(str[2:4])

# Negative indexing. p -6, y -5, t -4, h -3, o -2, n -1.

print(str[-4:-2])